# Variables de entorno para testing
PORT=3002
NODE_ENV=test

# Base de datos (usar la misma que desarrollo por ahora)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cooperativa_ugarte
DB_USER=cooperativa_user
DB_PASSWORD=cooperativa_pass

# JWT
JWT_SECRET=test_secret_key_jest
JWT_EXPIRATION=1h
